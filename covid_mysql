create database covid_stats;
show databases;
use covid_stats;
CREATE TABLE covid_data(
    Location varchar(255),
    Active_Cases int,
    Recovered int,
    Deaths int
);

**use link mentioned in readme for insertion in data in datable
INSERT INTO db_name.table_name(col1_name,col2_name......coln_name)
Values(v1,v2..............vn);
